    ---
name: html-report-generator
description: Generates a single self-contained, dark-themed HTML report that gives a quick overview of a target file plus a representative sample of its contents — first 5 rows of a CSV, first 50 lines of a log, function list for a Python file, outline for Markdown, etc. The output follows a rich documentation style with a gradient cover header, numbered sections, colored callouts, file-path labels, and badges. Use whenever the user asks to "generate a report," "create an HTML summary," "make an overview," "show me what's in this file," "build a report for X," or any similar request — even when "HTML" isn't explicitly mentioned. Trigger on requests to summarize, preview, or produce a shareable snapshot of any file.
---

# HTML Report Generator

Produce a single self-contained `.html` file that gives a **quick overview** of a target file plus a **representative sample** of its contents. The output must open in any browser with no external assets, and must use the **dark-themed component system** defined below.

## Workflow

1. **Identify the target file.** If the user named a path, use it. If not, ask before proceeding.
2. **Read the file** with the appropriate tool. Stat the size first; for files >10 MB, only read the first/last few KB.
3. **Detect the type** from extension + a first-line sniff (shebang, JSON/CSV cues, magic bytes).
4. **Build the report** using the section structure below and the matching per-type recipe.
5. **Write the output** to `./reports/<input-stem>-report.html`. Create `reports/` if missing. If a file with that name exists, append a timestamp.
6. **Confirm the path** and offer to open it in the browser.

## Section structure (every report has these in order)

1. **Cover header** (`<header class="cover">`) — gradient banner with the report title (filename) and a subtitle line describing what the report is. Include a `.meta` line with generation timestamp + file size.
2. **Intro callout** (`<div class="callout">`) — 1–3 sentences in plain English saying what the file appears to be and what this report contains.
3. **Section 1 — File information** — small metadata `<table>`: path, type, size, last-modified, encoding, line count.
4. **Section 2 — Overview** — type-specific stats. Use a `<table>` with the right-aligned numeric column for counts.
5. **Section 3 — Sample content** — the actual file slice. Always put a `.filebox` above any sample `<pre>` or `<table>` showing the source path the sample came from.
6. **Section 4 — Notes** — any warnings, redactions, parse errors, or observations. Use the appropriate `.callout-*` variant.
7. **Footer** — "Generated by html-report-generator" + ISO timestamp.

Use `<h2 id="sec-N">` for each section so anchors work. Skip the table-of-contents nav for short reports (4–5 sections); add it only if the report grows beyond ~8 sections.

## Per-type recipes

### CSV / TSV
- **Overview table**: row count, column count, column names with inferred types (number / string / date / bool), null-count per column
- **Sample**: `.filebox` with full path, then first 5 rows in a `<table>`. Use `.num` class on numeric columns, `.mono` on timestamp/ID columns. If >5 columns, wrap the table in a `<div style="overflow-x:auto">`.
- **Optional aggregate row**: if numeric columns exist, append a `<tr class="agg-row">` showing sum/mean for each numeric column.

### JSON
- **Overview table**: top-level type (object / array), key count or array length, max nesting depth, total size
- **Sample**: `.filebox` with path, then pretty-printed first 50 lines inside `<pre><code>`. If the root is an array of objects, follow with a small schema table (key, inferred type, sample value) using `<span class="badge badge-info">` for each type.

### Log files (.log, .txt with timestamps)
- **Overview**: total lines, time range (first/last timestamp if parseable), counts of ERROR / WARN / INFO each shown with the matching badge (`badge-warn` / `badge-warn` / `badge-info`)
- **Sample**: `.filebox` with path, then a `<pre>` showing first 10 lines, a `...` separator, and last 10 lines.
- **Top errors**: if errors are present, a small table of top 3 error messages with counts.

### Source code (Python / JS / TS / Go / Rust / Java / C / C++)
- **Overview table**: total lines, code vs comment vs blank, function count, class count, language
- **Function/class list**: `<table>` with columns "Kind" (badge: `badge-purple` for class, `badge-info` for function), "Name" (`.mono`), "Line" (`.num`)
- **Sample**: `.filebox` with path, then the first 30 lines of source in `<pre><code>`.
- **TODO/FIXME callout**: if any are found, render them in a `.callout-warn` with line numbers.

### Markdown / plain text
- **Overview**: word count, line count, estimated reading time (200 wpm), heading count by level
- **Outline**: nested `<ol>` of h1–h3 headings as anchor links
- **Sample**: `.filebox` with path, then first 200 words of body in a `<pre>` or styled paragraph.
- **Extras**: link count and image-reference count in the overview table.

### Unknown / binary
- **Overview**: file size, MIME-type guess
- **Sample**: `.filebox` with path, then first 256 bytes as a hex dump in `<pre>` (for binary), or first 30 lines in `<pre>` (for unknown text)
- **Note**: include a `.callout-warn` saying the type wasn't recognized and only a generic preview is shown.

## HTML/CSS — exact dark-theme template

Use this as the base. **Inline everything** — no external CSS or JS. The component vocabulary (callouts, filebox, badges, agg-row) is what makes reports look consistent.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Report: FILENAME</title>
  <style>
    :root {
      --bg: #0d1117;
      --panel: #161b22;
      --panel-2: #1c2128;
      --border: #30363d;
      --text: #e6edf3;
      --muted: #8b949e;
      --accent: #58a6ff;
      --green: #3fb950;
      --green-bg: #0d2818;
      --red: #f85149;
      --red-bg: #2d1316;
      --amber: #d29922;
      --amber-bg: #2d2206;
      --purple: #bc8cff;
      --purple-bg: #1f1535;
      --blue-bg: #0c2f4a;
      --code-color: #ff7b72;
    }
    * { box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
      line-height: 1.55;
      color: var(--text);
      background: var(--bg);
      margin: 0;
    }
    .container {
      max-width: 1240px;
      margin: 0 auto;
      padding: 32px 40px 80px;
    }
    header.cover {
      background: linear-gradient(135deg, #1f6feb 0%, #238636 100%);
      color: white;
      padding: 36px;
      margin: -32px -40px 28px;
      border-radius: 0 0 12px 12px;
    }
    header.cover h1 { margin: 0 0 8px; font-size: 26px; }
    header.cover .subtitle { margin: 0; font-size: 14px; opacity: 0.95; }
    header.cover .meta { margin-top: 10px; font-size: 12px; opacity: 0.85; }
    header.cover code {
      background: rgba(255,255,255,0.18);
      color: #fff;
      padding: 1px 6px;
      border-radius: 3px;
    }
    h2 {
      font-size: 21px;
      margin-top: 36px;
      padding-bottom: 8px;
      border-bottom: 2px solid var(--border);
      color: var(--text);
    }
    h3 { font-size: 16px; margin-top: 22px; color: var(--text); }
    h4 {
      font-size: 14px;
      margin-top: 18px;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.4px;
    }
    p { margin: 8px 0; }
    a { color: var(--accent); text-decoration: none; }
    a:hover { text-decoration: underline; }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 12px 0;
      font-size: 13px;
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 6px;
      overflow: hidden;
    }
    th, td {
      text-align: left;
      padding: 8px 10px;
      border-bottom: 1px solid var(--border);
      vertical-align: top;
    }
    th {
      background: var(--panel-2);
      font-weight: 600;
      font-size: 11.5px;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.3px;
    }
    tr:last-child td { border-bottom: none; }
    tr:hover td { background: var(--panel-2); }
    td.center, th.center { text-align: center; white-space: nowrap; }
    td.num, th.num {
      text-align: right;
      font-family: "SF Mono", Monaco, Consolas, monospace;
      font-size: 12.5px;
    }
    td.mono {
      font-family: "SF Mono", Monaco, Consolas, monospace;
      font-size: 12.5px;
    }

    code {
      font-family: "SF Mono", Monaco, Consolas, monospace;
      font-size: 12.5px;
      background: var(--panel-2);
      padding: 1px 6px;
      border-radius: 3px;
      color: var(--code-color);
    }
    pre {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 6px;
      padding: 14px 18px;
      overflow-x: auto;
      font-family: "SF Mono", Monaco, Consolas, monospace;
      font-size: 12.5px;
      line-height: 1.5;
      color: var(--text);
    }
    pre code { background: transparent; padding: 0; color: var(--text); }

    .badge {
      display: inline-block;
      padding: 2px 8px;
      font-size: 12px;
      font-weight: 600;
      border-radius: 4px;
      line-height: 1.4;
      white-space: nowrap;
    }
    .badge-ok    { background: var(--green-bg);  color: var(--green); }
    .badge-warn  { background: var(--amber-bg);  color: var(--amber); }
    .badge-info  { background: var(--blue-bg);   color: var(--accent); }
    .badge-purple{ background: var(--purple-bg); color: var(--purple); }
    .badge-err   { background: var(--red-bg);    color: var(--red); }

    .callout {
      background: var(--blue-bg);
      border-left: 4px solid var(--accent);
      padding: 12px 16px;
      margin: 12px 0;
      border-radius: 0 6px 6px 0;
      font-size: 14px;
    }
    .callout-ok {
      background: var(--green-bg);
      border-left: 4px solid var(--green);
      padding: 12px 16px;
      margin: 12px 0;
      border-radius: 0 6px 6px 0;
      font-size: 14px;
    }
    .callout-warn {
      background: var(--amber-bg);
      border-left: 4px solid var(--amber);
      padding: 12px 16px;
      margin: 12px 0;
      border-radius: 0 6px 6px 0;
      font-size: 14px;
    }
    .callout-tech {
      background: var(--purple-bg);
      border-left: 4px solid var(--purple);
      padding: 12px 16px;
      margin: 12px 0;
      border-radius: 0 6px 6px 0;
      font-size: 14px;
    }
    .callout-tech strong { color: var(--purple); }
    .simple {
      background: var(--panel-2);
      border-left: 4px solid var(--muted);
      padding: 12px 16px;
      margin: 12px 0;
      border-radius: 0 6px 6px 0;
      font-size: 14.5px;
    }
    .simple::before {
      content: "Plain English: ";
      font-weight: 700;
      color: var(--muted);
      font-size: 12px;
      letter-spacing: 0.5px;
      text-transform: uppercase;
    }

    nav.toc {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 6px;
      padding: 14px 18px;
      margin: 20px 0;
    }
    nav.toc h3 {
      margin: 0 0 8px;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--muted);
    }
    nav.toc ol { margin: 0; padding-left: 20px; columns: 2; column-gap: 32px; }
    nav.toc ol li { padding: 3px 0; break-inside: avoid; }

    .small { font-size: 12px; color: var(--muted); }

    .filebox {
      background: #010409;
      color: #f8f8f2;
      padding: 8px 14px;
      border-radius: 6px 6px 0 0;
      font-family: "SF Mono", Monaco, Consolas, monospace;
      font-size: 12px;
      margin-top: 12px;
      margin-bottom: 0;
      border: 1px solid var(--border);
      border-bottom: none;
    }
    .filebox + pre, .filebox + table {
      margin-top: 0;
      border-radius: 0 0 6px 6px;
      border-top: none;
    }

    .agg-row td {
      background: var(--amber-bg);
      color: var(--amber);
      font-weight: 600;
    }

    hr { border: none; border-top: 1px solid var(--border); margin: 32px 0; }
    footer {
      margin-top: 40px;
      padding-top: 16px;
      border-top: 1px solid var(--border);
      color: var(--muted);
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="cover">
      <h1>Report: FILENAME</h1>
      <p class="subtitle">Quick overview &amp; sample preview generated by html-report-generator</p>
      <p class="meta">Generated YYYY-MM-DD HH:MM:SS • Source: PATH • Size: N KB</p>
    </header>

    <!-- intro callout, sections 1..N, footer -->

    <footer>Generated by html-report-generator • TIMESTAMP</footer>
  </div>
</body>
</html>
```

## Component usage guide

Match each piece of report content to the right component:

- **`.callout`** (blue, info) — neutral observations about the file
- **`.callout-ok`** (green) — sanity checks that passed, "no issues found"
- **`.callout-warn`** (amber) — truncation, redactions, parse errors, missing data
- **`.callout-tech`** (purple) — technical caveats: encoding fallback, type guesses, version notes
- **`.simple`** (gray, "Plain English:" prefix) — the one-sentence description of what the file is
- **`.filebox`** (dark monospace bar) — always sits flush above a `<pre>` or sample `<table>`, showing the source path
- **`.badge-info`** (blue) — file types, neutral tags
- **`.badge-ok`** (green) — passed checks, healthy counts
- **`.badge-warn`** (amber) — warnings, ERROR / WARN log levels
- **`.badge-purple`** (purple) — inferred properties, derived types
- **`.badge-err`** (red) — errors, failed parses
- **`.agg-row`** (highlighted) — sum / mean / total row at the bottom of a numeric sample table
- **`td.num`** — right-aligned monospace for numeric columns
- **`td.mono`** — left-aligned monospace for IDs, timestamps, paths

## Safety rules

- **Redact likely secrets** in samples before embedding. Match patterns including: `AKIA[0-9A-Z]{16}`, `sk-[A-Za-z0-9]{20,}`, `xox[bp]-`, `Bearer\s+[A-Za-z0-9]+`, and any line where a key name contains `password`, `secret`, `token`, `api_key`, `apikey`, or `private_key`. Replace the value with `[REDACTED]` and add a `.callout-warn` in Notes mentioning the redaction.
- **Never embed raw bytes** from binary files beyond the 256-byte hex preview.
- **HTML-escape all content** from the source file before embedding — no raw `<`, `>`, `&`, or quotes leaking through.

## Edge cases

- **File >10 MB**: stat-only read; for samples, read just the first and last few KB. Add a `.callout-warn` about the truncation.
- **File doesn't exist**: tell the user, list nearby paths in the same directory, ask for clarification.
- **Encoding errors**: try UTF-8 first, fall back to latin-1; if neither parses cleanly, treat as binary and use the hex dump path. Mention the fallback in a `.callout-tech`.
- **Empty file**: produce a minimal report stating the file is empty — still write the HTML with cover header and a single `.callout-warn`.
- **Multiple files in one request**: generate one HTML per file, then offer to build an index page that links to all of them using the same dark theme.